# URL del backend per le chiamate API del frontend.
# Sviluppo locale: lasciare VUOTO -> le chiamate restano relative e usano il proxy di Vite.
# Produzione (Render Static Site): impostare al dominio del backend, es:
#   VITE_BACKEND_URL=https://ingegneria-software.onrender.com
VITE_BACKEND_URL=
